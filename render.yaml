services:
  - type: web
    name: feriados-api
    runtime: python
    plan: free
    buildCommand: "pip install -e ."
    startCommand: "uvicorn pt_holidays_api.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
